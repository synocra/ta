# Ultralytics YOLO11 - P2-only (Fixed Concat Resolution)
nc: 80

scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

backbone:
  - [-1, 1, Conv, [64, 3, 2]]              # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]             # 1-P2/4 ← ✅ resolusi H/4
  - [-1, 2, C3k2, [256, False, 0.25]]      # 2
  - [-1, 1, Conv, [256, 3, 2]]             # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]      # 4-P3 feature ← ✅ gunakan ini untuk upsample ke P2
  - [-1, 1, Conv, [512, 3, 2]]             # 5-P4/16
  - [-1, 2, C3k2, [512, True]]             # 6
  - [-1, 1, Conv, [1024, 3, 2]]            # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]            # 8
  - [-1, 1, SPPF, [1024, 5]]               # 9
  - [-1, 2, C2PSA, [1024]]                 # 10

head:
  # 🔸 Langsung upsample dari P3 (index 4) ke P2
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]   # 11 ← dari P5? TIDAK! Ganti source

  # ✅ Perbaiki: ambil P3 (index 4) sebagai dasar
  - [4, 1, nn.Upsample, [None, 2, "nearest"]]    # 11 ← upsample P3 (H/8 → H/4)
  - [[-1, 1], 1, Concat, [1]]                    # 12 ← concat dengan P2 (index 1) → H/4 + H/4 = ✅
  - [-1, 1, C3k2, [128, False]]                  # 13 ← P2 head

  # 🔹 Detection: hanya P2
  - [[13], 1, Detect, [nc]]                      # 14